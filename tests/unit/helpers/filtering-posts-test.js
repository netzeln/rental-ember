import { filteringPosts } from 'super-rentals/helpers/filtering-posts';
import { module, test } from 'qunit';

module('Unit | Helper | filtering posts');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = filteringPosts([42]);
  assert.ok(result);
});
